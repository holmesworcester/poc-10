# Initial poc-10 fact-module projection table inventory.
# These declarations list durable ownership without migrating behavior yet.

table message_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table opened_message_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table sealed_message_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table message_tombstone_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table file_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table file_slice_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table workspace_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table recipient_key_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table key_wrap_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table user_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table local_endpoint_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table local_endpoint_secret_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table local_endpoint_signing_public_key_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table local_endpoint_signing_secret_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table identity_endpoint_shared_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table content_event_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table cascade_staged_fact_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table admin_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table reaction_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table content_message_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table connection_ephemeral_secret_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table connection_request_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table connection_response_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table invite_accepted_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table invite_server_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table user_invite_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table device_invite_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table invite_secret_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table sync_compare_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table sync_have_id_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table sync_need_id_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table message_deletion_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table file_deletion_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table removal_frontier_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table local_history_node_secret_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}

table disappearing_messages_setting_rows {
  column key bytes;
  column value bytes;
  row_key (key);
}
