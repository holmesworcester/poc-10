//! Freestyle attempt: device-invite projector written as two parallel English
//! narratives, one per signing path. See `project.rs` for the idiom and the
//! full security policy. The invariant battery is wired up inside the
//! `tests` submodule of `project.rs`.

pub mod project;
